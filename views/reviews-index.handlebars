<h1>Reviews</h1>

<div class="row">
  {{#each reviews}}
    <div class="col-sm-3">
      <h2><a href="/reviews/{{this._id}}">{{this.title}}</a></h2>
      <font size ="+1">{{this.movieTitle}}</font>
    </div>
  {{/each}}
</div>
